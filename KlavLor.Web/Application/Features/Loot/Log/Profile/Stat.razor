<div class="flex flex-col">
    <span class="text-[10px] uppercase tracking-wider text-slate-500 dark:text-white">@Label</span>
    <span class="text-base font-semibold @ValueClass">@Value</span>
    @if (!string.IsNullOrEmpty(Sub))
    {
        <span class="text-[10px] text-slate-400 dark:text-white">@Sub</span>
    }
</div>

@code {
    [Parameter] public required string Label { get; set; }
    [Parameter] public required string Value { get; set; }
    [Parameter] public string? Sub { get; set; }
    [Parameter] public string? Accent { get; set; }

    private string ValueClass => Accent switch
    {
        "amber" => "text-amber-600 dark:text-amber-400",
        "cyan" => "text-cyan-700 dark:text-cyan-300",
        _ => "text-slate-900 dark:text-slate-100"
    };
}
